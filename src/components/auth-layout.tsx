import { Logo } from "./logo";
export function AuthLayout({ title, description, children }: { title: string; description: string; children: React.ReactNode }) { return <main className="auth-page"><aside className="auth-aside"><Logo/><div className="auth-quote"><blockquote>“Entender un error vale más que acertar por casualidad.”</blockquote><p>Práctica estructurada para avanzar con calma, precisión y evidencia.</p></div><small>PAES M1 · Admisión 2027</small></aside><section className="auth-main"><div className="auth-card"><p className="eyebrow">Savia M1</p><h1>{title}</h1><p className="muted">{description}</p>{children}</div></section></main>; }
